---
alwaysApply: true
description: 代码风格和命名规范
---

# 代码风格和命名规范

## Kotlin 代码风格

### 命名约定

- **类名**: PascalCase (`PhotoService`, `AppViewModel`)
- **函数/变量**: camelCase (`uploadPhoto`, `isLoggedIn`)
- **常量**: UPPER_SNAKE_CASE (`DEFAULT_TIMEOUT`)
- **包名**: 小写，点分隔 (`com.xichen.cloudphoto.service`)

### 文件组织

- 每个文件一个主要类/接口
- 相关扩展函数放在同一文件或单独的扩展文件
- 数据类可以放在同一文件（如果相关）

### 函数规范

```kotlin
// 好的：清晰的函数名和参数
suspend fun uploadPhoto(
    photoData: ByteArray,
    fileName: String,
    mimeType: String
): Result<Photo>

// 避免：模糊的函数名
suspend fun upload(data: ByteArray): Result<Photo>
```

### 注释规范

```kotlin
/**
 * 上传照片到云端存储
 * 
 * @param photoData 照片字节数据
 * @param fileName 文件名
 * @param mimeType MIME 类型（如 "image/jpeg"）
 * @return Result<Photo> 成功返回 Photo 对象，失败返回异常
 */
suspend fun uploadPhoto(...): Result<Photo>
```

## Compose 代码风格

### Composable 函数

```kotlin
@Composable
fun MyScreen(
    viewModel: AppViewModel = viewModel(),
    modifier: Modifier = Modifier
) {
    // 实现
}
```

### 状态收集

```kotlin
val photos by viewModel.photos.collectAsState()
val isLoading by viewModel.isLoading.collectAsState()
```

### 参数顺序

1. 必需参数（ViewModel、回调）
2. `modifier`（如果有）
3. 可选参数

## 导入顺序

1. Kotlin 标准库
2. Android/平台特定
3. Compose
4. 第三方库
5. 项目内部

```kotlin
import kotlinx.coroutines.flow.StateFlow
import android.content.Context
import androidx.compose.foundation.layout.*
import io.ktor.client.HttpClient
import com.xichen.cloudphoto.model.Photo
```

## 代码格式

### 缩进

- 使用 4 个空格（不是 Tab）
- 函数参数换行时，第一个参数在新行

```kotlin
fun longFunction(
    param1: String,
    param2: Int,
    param3: Boolean
): Result<Unit>
```

### 空行

- 类/函数之间：1 个空行
- 逻辑块之间：1 个空行
- 导入和代码之间：1 个空行

### 大括号

```kotlin
// 好的
if (condition) {
    doSomething()
}

// 单行可以省略
if (condition) return
```

## 类型安全

### 使用具体类型

```kotlin
// 好的
val photos: List<Photo> = emptyList()

// 避免
val photos: List<*> = emptyList()
```

### 空安全

```kotlin
// 好的
val name: String? = getName()
name?.let { println(it) }

// 避免
val name: String? = getName()
if (name != null) println(name)  // 虽然可以，但 prefer let
```

## 错误处理

### 使用 Result

```kotlin
// 好的
suspend fun doSomething(): Result<Unit> {
    return try {
        // 操作
        Result.success(Unit)
    } catch (e: Exception) {
        Result.failure(e)
    }
}

// 避免
suspend fun doSomething() {
    // 直接抛出异常
    throw Exception("Error")
}
```

## 性能优化

### 懒加载

```kotlin
// 好的
val service: PhotoService by lazy { PhotoService(...) }

// 避免（除非需要立即初始化）
val service: PhotoService = PhotoService(...)
```

### 集合操作

```kotlin
// 好的：使用序列处理大数据集
val result = largeList.asSequence()
    .filter { it.isValid }
    .map { it.transform() }
    .toList()

// 避免：链式操作创建中间集合
val result = largeList
    .filter { it.isValid }
    .map { it.transform() }
```

## 文档注释

### KDoc 格式

```kotlin
/**
 * 上传照片到云端存储服务
 * 
 * @param photoData 照片的字节数组
 * @param fileName 文件名，将用于存储
 * @param mimeType MIME 类型，如 "image/jpeg"
 * @return [Result] 包含上传后的照片 URL，失败时包含异常
 * 
 * @throws [NetworkError] 当网络请求失败时
 * @throws [StorageError] 当存储服务返回错误时
 * 
 * @sample
 * ```
 * val result = photoService.uploadPhoto(bytes, "photo.jpg", "image/jpeg")
 * result.onSuccess { url -> println("Uploaded to: $url") }
 * ```
 */
suspend fun uploadPhoto(...): Result<String>
```

## 最佳实践

1. **可读性优先**：代码应该易于理解
2. **一致性**：遵循项目已有的代码风格
3. **简洁性**：避免过度复杂的表达式
4. **类型安全**：充分利用 Kotlin 的类型系统
5. **文档**：公共 API 必须有文档注释
